pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'npm install'
      }
    }
    stage('Delivery') {
      when {
        branch 'master'
      }
      steps {
      	docker.withRegistry('https://hub.docker.com', 'dockerhub') {
        	def image = docker.build("vladobrdaric/express-calculator")
		image.push()
      	}
      }
    }
  }
}
